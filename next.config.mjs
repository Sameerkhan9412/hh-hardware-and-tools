/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["res.cloudinary.com","images.unsplash.com","assets.aceternity.com"], // Add Cloudinary domain
      },
};

export default nextConfig;
